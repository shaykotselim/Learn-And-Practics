#include<stdio.h>
    int main(){

        // Declar variable type ;

       int radius;
        float  result, pi,volume, r3;

        // Declar variable;

         volume = 4/3;
         pi = 3.1416;
         radius = 20;
         r3 = radius * radius * radius;

        // result
         result = volume * pi * r3;

        printf("%f",result);
        return 0;

    }
